name: liquid_glass_example
description: Example app for the liquid_glass package.
publish_to: 'none'

version: 0.1.0

environment:
  sdk: ">=2.19.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  liquid_glass:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
